<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- Attempting red color text for logout button here -->
        <!-- Did not work <item name="android:actionMenuTextColor">@color/colorLogoutText</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorTextSelect</item>
    </style>

    <style name="RedText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#FF0000</item>
    </style>
</resources>
